# Legacy unit manifest

The medieval external-unit manifest is intentionally empty. `Imperios del
Enjambre` loads only original insect assets generated by the repository's own
Blender script from `../insects/manifest.json`; the TypeScript procedural
insects remain the zero-network fallback.
